@html.extend('layout', function(model) {
  @html.block('content', function(model) {
    <h1>@model.title</h1>
    <i style="font-size:xx-small;">Published on @model.post.createtimestamp</i>
    @html.include(`p/${model.post.name}`, model)
  })
})